#include<stdio.h>
typedef int km_per_hour;
typedef int points;
void congratulation(points high_score){
    printf("Yes");
}
int main(){
    km_per_hour current_speed=10;
    points high_score=20;
    congratulation(high_score);
}
